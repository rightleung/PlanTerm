# PlanTerm v0.1.1：FP&A Credibility Hardening

## Summary

在不加入产品分类、CSV 上传或情景编辑的前提下，加固现有 MINISO 案例的数据可信度、管理洞察、筛选行为和 Excel Management Pack。

使用分支 `codex/v0.1.1-hardening`，版本升级至 `0.1.1`。不得覆盖或移动现有 `v0.1.0` tag。

## Implementation Changes

### 1. 改进业务单元利润分摊

- 在 `assumptions.json` 增加明确标记为模拟的利润分摊指数：
  - Gross-margin index：Mainland `0.96`、Overseas `1.04`、TOP TOY `1.10`
  - Operating-margin index：Mainland `0.90`、Overseas `1.08`、TOP TOY `1.20`
- 对 FY2025、2025 H1、2026 H1 分别以 `revenue × index` 计算初始权重，再归一化至公开集团 Gross Profit 和 Operating Profit。
- 保证业务单元利润率有所区分，同时集团 Revenue、Gross Profit、Operating Profit 继续精确对账。
- 不得把分配后的业务单元利润标成 `public_reported`；继续使用 `synthetic_allocation`。
- 如果分配结果出现负成本、负 Opex、Gross Profit 高于 Revenue，生成器必须失败，不允许静默修正。

### 2. 修正驱动因素与洞察逻辑

- `primary_driver` 保留以兼容 v0.1 API，但其含义固定为最不利的 Revenue PVM driver。
- `VarianceRow` 新增：
  - `gross_margin_actual`
  - `gross_margin_budget`
  - `operating_profit_variance`
  - `profit_driver`
  - `profit_driver_amount`
- 计算可对账的 Operating Profit driver：
  - Price/Volume/Mix profit effect = 对应 PVM amount × Budget gross margin
  - Gross Margin effect = Actual revenue × `(Actual GM − Budget GM)`
  - Opex effect = `−(Actual Opex − Budget Opex)`
- 所有利润影响之和必须等于 Actual vs Budget Operating Profit variance，容差 RMB 0.01m。
- 洞察分别说明 Revenue driver 和 Operating Profit driver，不再把收入影响和费用金额直接比较。
- 表格中的 `Primary driver` 改名为 `Revenue driver`；洞察行动建议根据 `profit_driver` 输出。

### 3. 修正筛选和空状态

- API 的 `available_filters` 增加 `valid_combinations`，由案例记录生成，不在前端重复维护业务单元映射。
- 前端根据合法组合禁用不适用的 Market：
  - MINISO：all/mainland/overseas
  - TOP TOY：all/global
- 切换 Brand 后，如果当前 Market 不再合法，自动重置为 `all`，只发出一次有效 API 请求。
- 直接请求合法取值但不兼容的组合时，API 返回 422：
  - `error_type=incompatible_filters`
  - `details` 包含 brand、market 和合法组合。
- 增加 Dashboard 级空状态；没有业务单元时不显示 KPI、PVM 或“没有不利差异”，并禁用 Excel 导出。
- PVM 不得再用 `null || 0` 生成虚假的 `+0` revenue variance。

### 4. 完善公开数据刷新

- 扩展 H1 parser，同时解析 MINISO Mainland、MINISO Overseas、TOP TOY 和 Others revenue split。
- 更新前验证：
  - Split 合计等于新解析的 Revenue。
  - 所有分项为有限非负数。
  - 来源 URL、period end、币种和单位正确。
- Dry-run 同时显示集团指标和 revenue split 的差异。
- `--write` 必须在同一次原子更新中写入 metrics、revenue split、source date 和 provenance。
- 页面缺少 split 或结构变化时明确失败，不保留旧 split 配合新 Revenue。

### 5. 重做 Excel Management Pack 格式

- 建立按工作表配置的格式函数，不再统一写死第 5 行：
  - Executive Summary：header row 6
  - 其余四张表：header row 2
- 每张表独立设置 freeze row、auto filter、列宽、数字格式和表头颜色。
- Executive Summary 增加 FY Budget、FY Forecast、Forecast Gap 和 Status。
- Variance、Variance %、YoY %、Forecast Gap 与 PVM reconciliation 使用带缓存结果的 Excel 公式。
- Business Unit Variance 增加 Gross Margin、Operating Profit Variance、Revenue Driver 和 Profit Driver。
- Favorable/Unfavorable 使用一致的绿色/红色条件格式。
- Assumptions & Sources 明确列出新的利润分摊指数及其 `Synthetic allocation` 属性。
- 导出继续严格反映当前合法筛选组合。

## Public Interfaces

- `GET /api/v1/cases/{case_id}/dashboard` 保持原路径和原字段。
- `available_filters.valid_combinations` 新增 `{brand, market, business_unit}` 列表。
- `VarianceRow` 只新增字段，不删除 v0.1 字段。
- 新增 `incompatible_filters` 422 错误，不改变未知 case 和非法枚举值的现有错误行为。
- Python、前端和 Excel 版本显示统一升级到 `0.1.1`。

## Test Plan

- 验证三个 Actual 业务单元的 GM/OM 不再完全相同。
- 验证 FY2025、2025 H1、2026 H1 的集团 Revenue、GP、OP 全部对账。
- 验证每个业务单元的 profit driver bridge 与 Operating Profit variance 对账。
- 验证收入洞察不会把 Opex 当作 Revenue PVM driver。
- 验证所有合法筛选组合，以及不兼容组合的 422 响应。
- Playwright 验证禁用选项、自动重置、无重复错误请求和筛选后 Excel 内容。
- Parser fixture 覆盖 revenue split 正常、缺字段、合计不符和页面结构变化。
- ExcelJS 重新打开工作簿并验证：
  - 五张工作表
  - 各自正确的 header/freeze/auto-filter
  - 公式、缓存结果、百分比格式和状态颜色
  - 当前筛选下的行数与 Dashboard 一致
- 更新 README 截图后运行完整 release gate：
  - `./.venv/bin/python -m pip check`
  - `./.venv/bin/python -m pytest -q`
  - `./.venv/bin/python scripts/build_miniso_case.py --check`
  - `npm run lint`
  - `npm run build`
  - `npm run e2e:preflight`

## Release and Acceptance

- 更新 README、ROADMAP 和数据方法说明，明确利润率属于归一化模拟分配。
- 合并前确认页面和输出不存在 RiskLens、信用评级或 covenant 残留。
- 合并 `codex/v0.1.1-hardening` 至 `main`。
- 创建 annotated tag `v0.1.1`，推送分支、main 和 tag。
- 确认 GitHub Actions 全部成功。
- 完成 v0.1.1 后再启动 v0.2；本阶段不实现产品分类、CSV 上传或情景编辑。
